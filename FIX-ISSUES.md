# حل مشاكل 404 في Next.js

إذا واجهت مشاكل 404 للملفات (layout.css, main-app.js, etc.)، جرب الخطوات التالية:

## الحل السريع:

1. **استخدم ملف clean-start.bat** (الأفضل):
   ```bash
   clean-start.bat
   ```

2. **أو قم بالخطوات يدوياً**:
   ```bash
   # أوقف جميع عمليات Node
   taskkill /F /IM node.exe
   
   # احذف مجلد .next
   rmdir /s /q .next
   
   # شغل السيرفر مرة أخرى
   npm run dev
   ```

## أسباب المشكلة:

- ملفات `.next` القديمة أو التالفة
- السيرفر لم يكمل البناء بعد
- مشاكل في التخزين المؤقت
- عمليات Node متعددة تعمل على نفس البورت

## الحلول:

### الحل 1: استخدم clean-start.bat
هذا الملف يقوم بتنظيف كل شيء وإعادة التشغيل بشكل نظيف.

### الحل 2: تنظيف يدوي
1. أغلق جميع نوافذ Terminal
2. احذف مجلد `.next`
3. شغل `npm run dev` من جديد
4. انتظر حتى يظهر "Ready" في Terminal

### الحل 3: تغيير البورت
إذا كان البورت 3000 مستخدم:
```bash
npm run dev -- -p 3001
```

## التحقق من الحل:

بعد التشغيل، يجب أن:
1. لا توجد أخطاء في Terminal
2. الصفحة تفتح بدون أخطاء 404
3. CSS والـ JavaScript يعملان بشكل صحيح

## إذا استمرت المشكلة:

1. تأكد من أن Node.js محدث
2. احذف `node_modules` وثبت من جديد:
   ```bash
   rmdir /s /q node_modules
   npm install
   ```
3. احذف `package-lock.json` وأعد التثبيت:
   ```bash
   del package-lock.json
   npm install
   ```

